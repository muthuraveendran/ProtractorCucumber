/**
 * @desc
 *  Wraps Webdriver promise into a Node-native promise to allow for correct promise chaining.
 *
 * @private
 *
 * @param promiseLike
 * @returns {Promise<T>}
 */
export declare function promiseOf<T>(promiseLike: PromiseLike<T>): Promise<T>;
