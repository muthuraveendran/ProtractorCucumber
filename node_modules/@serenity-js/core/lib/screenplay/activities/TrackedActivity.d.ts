import { Stage } from '../../stage';
import { Activity } from '../Activity';
import { AnswersQuestions, PerformsActivities, UsesAbilities } from '../actor';
import { ActivityDescriber } from './ActivityDescriber';
import { OutcomeMatcher } from './OutcomeMatcher';
/** @package */
export declare class TrackedActivity implements Activity {
    protected readonly activity: Activity;
    protected readonly stage: Stage;
    protected static readonly describer: ActivityDescriber;
    protected static readonly outcomes: OutcomeMatcher;
    constructor(activity: Activity, stage: Stage);
    performAs(actor: (PerformsActivities | UsesAbilities | AnswersQuestions) & {
        name: string;
    }): PromiseLike<void>;
    toString(): string;
}
