import { JSONObject } from 'tiny-types';
import { CorrelationId, Description, Timestamp } from '../model';
import { DomainEvent } from './DomainEvent';
export declare class AsyncOperationCompleted extends DomainEvent {
    readonly taskDescription: Description;
    readonly correlationId: CorrelationId;
    static fromJSON(o: JSONObject): AsyncOperationCompleted;
    constructor(taskDescription: Description, correlationId: CorrelationId, timestamp?: Timestamp);
}
