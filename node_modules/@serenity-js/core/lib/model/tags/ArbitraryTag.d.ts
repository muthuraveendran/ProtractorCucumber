import { Tag } from './Tag';
/**
 * @desc
 *  Tagged with an arbitrary tag that doesn't have any special interpretation
 *  such as @regression, @wip, etc.
 *
 * @access public
 */
export declare class ArbitraryTag extends Tag {
    static readonly Type = "tag";
    constructor(value: string);
}
